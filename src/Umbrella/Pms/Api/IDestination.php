<?php

namespace Umbrella\Pms\Api;

/**
 * A Destination object encapsulates a provider-specific address.
 * The PMS API does not define a standard address syntax. 
 * Although a standard address syntax was considered, it was decided 
 * that the differences in address semantics between 
 * existing message-oriented middleware (MOM) products were too 
 * wide to bridge with a single syntax.
 * 
 * @author Italo Lelis de Vietro <italolelis@lellysinformatica.cm>
 */
interface IDestination
{
    
}
